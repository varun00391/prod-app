# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://varunsingh2191_db_user:<db_password>@cluster0.sedm64q.mongodb.net/?appName=Cluster0   #mongodb+srv://user:password@cluster.mongodb.net/chatgpt_app?retryWrites=true&w=majority
MONGODB_DB=chatgpt_app

# Optional: Redis for caching (ElastiCache)
# REDIS_URL=redis://your-elasticache-endpoint:6379

# Optional: Default Groq key (otherwise use per-request API key from frontend)
# GROQ_API_KEY=gsk_...

# JWT for auth (change in production)
# JWT_SECRET=your-secret-at-least-32-chars
